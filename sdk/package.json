{
  "name": "@d-hrs/sdk",
  "version": "1.0.0",
  "description": "D-HRS SDK - Decentralized HR System",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "typescript": "^5.3.0"
  }
}
